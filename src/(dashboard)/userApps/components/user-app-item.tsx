interface UserAppItemProps {
  id: number;
  name: string;
  description: string;
  profileUrl: string;
  logoUrl: string;
}

export const UserAppItem = ({
  id,
  name,
  description,
  profileUrl,
  logoUrl,
}: UserAppItemProps) => {
  return <></>;
};
